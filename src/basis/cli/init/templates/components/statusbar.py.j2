"""
The app's status bar — app-owned chrome (generated).

Keeps the shell tag (`shell-status-bar`) and replaces it in the registry, so
the frame's `<shell-status-bar>` renders this app version. The `template()`
below is a copy of the shell's — edit it here to restyle the bar without
touching the framework. Inherited props and `style()` come from the shell's
`StatusBar`; override them here when needed.
"""
from basis.plugins.shell.status_bar import StatusBar


class MyStatusBar(StatusBar):
    height = "{{ statusbar_height }}"

    def template(self):
        """
        <div class="shell-status-bar" style="flex: 0 0 {height};" data-border="{border}">
            <shell-stack direction="{direction}" gap="{gap}" align="{align}" justify="{justify}" size="1 1 auto">
                <slot></slot>
            </shell-stack>
        </div>
        """
